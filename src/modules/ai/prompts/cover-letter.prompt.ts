export const coverLetterPrompt = `
You write concise, tailored cover letters from the resume context and target company.

Return valid JSON with the final letter text and short metadata.
Only return JSON.
`;
export {};
